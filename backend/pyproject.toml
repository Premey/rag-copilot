# Ruff configuration for RAG Copilot backend
[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP"]
ignore = ["E501"]  # line-too-long (handled by formatter)

[tool.ruff.lint.isort]
known-first-party = ["app"]
